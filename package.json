{
  "name": "menu-reveal",
  "version": "1.0.0",
  "description": "Exercise to use animations with gsap ",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:parcel": "parcel build src/index.html --no-content-hash --no-source-maps --public-url ./",
    "build": "npm run clean && npm run build:parcel",
    "start": "parcel src/index.html --open"
  },
  "author": "Tugrul Yuksel",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.0.1"
  },
  "dependencies": {
    "gsap": "^3.8.0",
    "imagesloaded": "^4.1.4",
    "split-type": "^0.2.5"
  }
}
